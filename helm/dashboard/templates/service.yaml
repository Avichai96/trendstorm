apiVersion: v1
kind: Service
metadata:
  name: {{ include "trendstorm-dashboard.fullname" . }}
  labels:
    {{- include "trendstorm-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "trendstorm-dashboard.selectorLabels" . | nindent 4 }}
